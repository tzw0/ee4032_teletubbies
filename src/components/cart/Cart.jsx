import './cart.scss'

export default function Cart() {
    return (
        <div className="cart">
            cart (page includes the products in cart, option to increase/decrease/remove item and the checkout mechanism). Cart is can be an express interest indicator
        </div>
    )
}
